.sidebar
  .sidebar__usename
    .sidebar__usename__top-items
      %p.sidebar__usename__top-items__name
        = current_user.name
      %ul.sidebar__usename__top-items__icons
        = link_to new_group_path(current_user) do
          = icon('far', 'edit', class: 'icon')
          %span<>
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog', class: 'icon')
          %span<>
  .sidebar__chatlist
    - current_user.groups.each do |group|
      .sidebar__chatlist__group
        = link_to '#', class:"group" do
          .sidebar__chatlist__group__name
            = group.name
          .sidebar__chatlist__group__message
            メッセージはまだありません。

